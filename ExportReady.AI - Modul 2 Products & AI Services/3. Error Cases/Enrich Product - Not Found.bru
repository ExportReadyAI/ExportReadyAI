meta {
  name: Enrich Product - Not Found
  type: http
  seq: 3
}

post {
  url: {{base_url}}/products/99999/enrich/
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

script:post-response {
  test('Returns 404 Not Found', function () {
      expect(res.getStatus()).to.eql(404);
  });
}

settings {
  encodeUrl: true
}

docs {
  Test 404 handling for enrichment on non-existent product
}
