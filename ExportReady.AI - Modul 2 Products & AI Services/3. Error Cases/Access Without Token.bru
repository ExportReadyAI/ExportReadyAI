meta {
  name: Access Without Token
  type: http
  seq: 4
}

get {
  url: {{base_url}}/products/
  body: none
  auth: inherit
}

script:post-response {
  test('Returns 401 Unauthorized', function () {
      expect(res.getStatus()).to.eql(401);
  });
}

settings {
  encodeUrl: true
}

docs {
  Test authentication guard - no token provided
  
  Expected: 401 Unauthorized
}
